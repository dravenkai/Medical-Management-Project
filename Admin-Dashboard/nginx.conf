server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # react-router client-side routing — always fall back to index.html
    # instead of nginx 404ing on a deep link like /inventory.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
